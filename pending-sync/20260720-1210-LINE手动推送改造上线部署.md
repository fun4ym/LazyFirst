# 20260720 LINE 手动推送改造 · 上线部署 + 自测记录

## 本次上线内容
1. **新品/活动推送改人工触发**：移除创建/更新时的自动 LINE 推送，改为前端按钮手动触发（按标签批量选达人）
2. **发送记录弹层**：新增 `LinePushRecord` 模型 + `GET /line/push-records` 接口，挂在活动列表
3. **申样通知仅推卖家**：移除达人端 `sampleConfirmedCard` 推送，仅保留卖家 `sampleApprovalCard`
4. **Flex 卡片重设计**：`newProductCard`(紫)/`productFlexCard`(紫)/`campaignCard`(深紫)/`sampleApprovalCard`(橙)，新增 `infoRow` 辅助函数

## 部署步骤（已全部完成）
1. `git rm --cached .env .DS_Store`（.env 曾被强制跟踪含 LINE 令牌，取消跟踪防泄露）→ commit → push origin main（22 文件）
2. 备份线上库：`tapdb_backup_20260720_120604.archive`（1.9M）
3. 本地 `npm run build`（dist 生成）
4. rsync 到服务器（dist 不 exclude，exclude node_modules/.git/.env/ssl）
5. `docker compose build --no-cache && up -d`（前后端重建）
6. 容器内跑 `migrate_line_fields.js`：Influencer 2019 / ShopContact 36 / Activity 11 / Product 357，索引已建

## 自测结果
- 独立 HTTP 自测脚本一轮：**35/35 通过**（前端200、受保护路由全401、公开接口非500、新 LINE 接口 /line/push-records 与 /line/product/:id/push 均 401 鉴权生效）
- 注：容器为生产镜像无 supertest，原 smoke-test.cjs 无法容器内跑，改用独立 HTTP 轮询等效自测

## 卡片重发验收
- 库中仅 2 个真实达人绑定 LINE（Uab5b82... / U70bfdd...），管理员账号未绑定
- 经主人同意，4 张重设计卡片各推到这 2 个已绑定 userId，8 条全部发送成功
- 使用真实商品数据渲染（[ACA] Botare 纸巾），临时脚本已清理

## 遗留/建议
- 主人个人 LINE 未以达人身份绑定，后续如需专属验收可在 App 绑定后单独推
- 如需保留"创建时自动推一次"可加回（当前默认关）

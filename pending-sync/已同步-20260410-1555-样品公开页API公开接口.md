# 样品公开页API公开接口

## 修改文件
- `server/routes/base-data.js` - 添加公开路由
- `server/server.js` - 挂载 /api/public/base-data
- `server/routes/public-samples.js` - 已存在的公开样品接口

## 修改内容
PublicCollection.vue 是独立页面不需要token，但原代码直接调用需要认证的/api/base-data。
解决方案是在后端添加公开路由 /api/public/base-data。

## 同步状态
✅ 已同步（代码已提交git）

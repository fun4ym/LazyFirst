# 2026-04-20 线上部署完成记录

## 部署时间
2026-04-20 17:47 (UTC+8)

## 前置操作
- 全量数据库备份：`/home/ubuntu/db-backups/backup-20260420-1641/`
- git提交：`9a5780f` 已推送到 main

## 部署步骤
1. rsync后端代码 → `/home/ubuntu/tap-system/server/`
2. rsync前端代码 → `/home/ubuntu/tap-system/frontend/`
3. scp Dockerfile、docker-compose.yml、nginx.conf
4. 本地 `npm run build` 前端dist → rsync dist到线上
5. `sudo docker build --no-cache` 后端+前端镜像
6. `sudo docker compose down && up -d` 重启所有容器

## 验证结果
- ✅ 3个容器正常运行（tap-backend, tap-frontend, tap-mongodb）
- ✅ 后端API响应正常（MongoDB连接成功）
- ✅ 前端页面可访问

## 本次部署核心修复
1. **Mongoose 8 ObjectId 必须用 new**（6处修复，解决400/500错误）
2. **编辑样品时salesmanId/influencerId空值处理**（解决400"业务员ID处理失败"）
3. 视频管理页面UI优化
4. BD匹配逻辑重构
5. 新增公开视频+商品统计接口

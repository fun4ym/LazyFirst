# 本地前后端数据库信息总结

**创建时间**：2026-04-13 19:56
**更新状态**：已写入MEMORY.md

## 总结内容

### 一、本地数据库信息
1. **数据库名称**：`tap_system`
2. **连接URI**：
   - 本地开发：`mongodb://localhost:27017/tap_system`
   - Docker生产：`mongodb://tapsystem:tap_system_pass_2024@mongodb:27017/tap_system?authSource=tap_system`
3. **数据量**：
   - users: 18条记录（包含admin用户）
   - samplemanagements: 6030条记录
4. **用户信息**：
   - admin: 管理员账户，密码 `ad8889`
   - BD用户: eye, sa, cin, film, nam, dingyan, sun, yuzi, test, moji, diana, xs, taa
   - inactive用户: ice, tee, poppy, ink

### 二、本地开发配置
1. **前端端口**：5174（vite.config.js中配置）
2. **后端端口**：3000（.env中配置）
3. **前端代理**：`http://localhost:5174/api` → `http://localhost:3000`

### 三、本地启动步骤
#### 方案A：纯本地开发（推荐）
1. 启动MongoDB本地服务
2. 启动后端服务：`cd server && npm run dev`
3. 启动前端服务：`cd frontend && npm run dev`
4. 访问地址：http://localhost:5174

### 四、数据库操作命令
1. **连接**：`mongosh tap_system`
2. **备份**：`mongodump --db tap_system --out ./backup_$(date +%Y%m%d)`
3. **恢复**：`mongorestore --db tap_system ./backup_20260413/`
4. **常用查询**：查看MEMORY.md第十八节详细命令

### 五、环境切换注意事项
1. **本地→线上**：修改.env中的MONGODB_URI为服务器地址
2. **线上→本地**：修改.env中的MONGODB_URI为本地地址

## 位置
- **主要信息**：MEMORY.md 第十八节 "本地开发环境配置"
- **文件路径**：/Users/mor/CodeBuddy/LazyFirst/MEMORY.md

## 目的
方便小垃圾以后快速了解本地开发环境配置，避免混淆本地和线上数据库，确保操作正确性。

## 验证
- [x] 已写入MEMORY.md
- [x] 无linter错误
- [x] 信息完整准确
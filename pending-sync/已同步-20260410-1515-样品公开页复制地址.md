# 待同步修改记录

**日期**: 2026-04-10
**状态**: 待同步

---

## 修改文件

1. `frontend/src/views/samples/PublicCollection.vue`

---

## 修改内容

### 1. 样品公开页面添加复制地址功能

**新增功能**：
- 列表中每个样品显示地址和复制图标按钮
- 点击复制图标可一键复制样品地址
- 使用 Element Plus `CopyDocument` 图标

**新增代码**：
```javascript
// 复制地址函数
const copyAddress = async (address) => {
  try {
    await navigator.clipboard.writeText(address)
    ElMessage.success('地址已复制')
  } catch (err) {
    ElMessage.error('复制失败')
  }
}
```

### 2. 修改寄样状态对话框重新设计

**改进点**：
- 使用 `el-form` + `el-form-item` 组件，布局规范统一
- 统一 `label-width="100px"`
- 拒绝原因添加 textarea 输入框
- 物流公司和快递单号在寄样状态时动态显示
- 表单验证：快递单号在选择非default物流公司时必填

**表单结构**：
- 寄样状态选择（el-select）
- 物流公司选择（寄样时显示）
- 快递单号输入（寄样时显示，required验证）
- 拒绝原因输入（拒绝时显示，textarea）

---

## 同步检查清单

- [ ] /samples/public 页面加载正常
- [ ] 每个样品显示地址
- [ ] 复制图标可见
- [ ] 点击复制图标后地址复制成功
- [ ] 提示"地址已复制"
- [ ] 修改寄样状态按钮可见
- [ ] 寄样状态对话框使用 el-form 布局
- [ ] 拒绝原因 textarea 正常显示
- [ ] 快递单号验证正常

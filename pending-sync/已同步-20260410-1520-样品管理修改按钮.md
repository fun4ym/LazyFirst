# 待同步修改记录

**日期**: 2026-04-10
**状态**: 待同步

---

## 修改文件

1. `frontend/src/views/samples/Management.vue`

---

## 修改内容

### 样品管理增加修改功能

**新增功能**：
- 操作列增加「修改」按钮
- 复用新建样品对话框，支持编辑已有样品
- 编辑模式时表单预填充现有数据

**实现方式**：
- 添加 `editingSample` 状态跟踪正在编辑的样品
- 添加 `openEditDialog(sample)` 函数打开编辑对话框
- 修改 `handleCreate` 为 `handleSubmit`，区分新建/编辑提交逻辑
- 添加 `resetCreateForm()` 函数重置表单

**新增按钮**：
```html
<el-button link type="warning" @click="openEditDialog(row)">
  {{ $t('samples.edit') || '修改' }}
</el-button>
```

**编辑对话框标题**：
- 新建：`samples.createSample`
- 编辑：`samples.editSample` || '修改样品'

---

## 同步检查清单

- [ ] /samples 页面加载正常
- [ ] 操作列显示「修改」按钮
- [ ] 点击修改按钮弹出编辑对话框
- [ ] 编辑对话框显示样品现有数据
- [ ] 修改后点击保存，数据更新成功
- [ ] 新建样品功能仍然正常

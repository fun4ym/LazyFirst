# 修复 samplePublic.sampleDetail 弹层国际化问题

## 修改时间
2026-04-14 14:47

## 修改文件
1. `frontend/src/views/samples/PublicCollection.vue`
2. `frontend/src/i18n/zh.js`
3. `frontend/src/i18n/en.js`
4. `frontend/src/i18n/th.js`

## 修改内容

### 1. PublicCollection.vue 修改

#### 问题一：添加多语种国际化
- 将标签页硬编码中文标签改为国际化翻译键：
  - "基础信息" → `$t('samplePublic.basicInfo')`
  - "寄样状态" → `$t('samplePublic.shippingStatus')`
  - "修改寄样状态" → `$t('samplePublic.modifyShippingStatus')`
  - "投流信息" → `$t('samplePublic.adInfo')`
  - "投流状态：" → `$t('samplePublic.adStatusDetail')`
  - "流码：" → `$t('samplePublic.streamCodeDetail')`
  - "当前状态：" → `$t('samplePublic.currentStatus')`
  - "当前物流信息" → `$t('samplePublic.currentShippingInfo')`
  - "当前拒绝原因" → `$t('samplePublic.currentRefusalReason')`
  - "物流公司：" → `$t('samplePublic.logisticsCompany')`
  - "运单号：" → `$t('samplePublic.trackingNumber')`
  - "寄出日期：" → `$t('samplePublic.shippingDate')`
  - "TikTok ID：" → `$t('samplePublic.tiktokIdDetail')`
  - "申请日期：" → `$t('samplePublic.applicationDate')`
  - "商品名称：" → `$t('samplePublic.productNameDetail')`
  - "商品ID：" → `$t('samplePublic.productIdDetail')`
  - "商品图片：" → `$t('samplePublic.productImage')`
  - "审批状态：" → `$t('samplePublic.approvalStatus')`
  - "BD：" → `$t('samplePublic.bd')`

#### 问题二：默认展示"寄样状态"页签
- 添加 `detailActiveTab` 变量定义：`const detailActiveTab = ref('shipping')`
- 在 `openStatusEdit` 函数中添加重置：`detailActiveTab.value = 'shipping'`

### 2. 国际化文件修改

#### zh.js (中文)
在 `samplePublic` 部分添加缺失的翻译键：
- `adInfo: '投流信息'`
- `currentStatus: '当前状态'`
- `currentShippingInfo: '当前物流信息'`
- `currentRefusalReason: '当前拒绝原因'`
- `approvalStatus: '审批状态'`

#### en.js (英文)
在 `samplePublic` 部分添加缺失的翻译键：
- `adInfo: 'Ad Information'`
- `currentStatus: 'Current Status'`
- `currentShippingInfo: 'Current Shipping Info'`
- `currentRefusalReason: 'Current Refusal Reason'`
- `approvalStatus: 'Approval Status'`

#### th.js (泰文)
在 `samplePublic` 部分添加缺失的翻译键：
- `adInfo: 'ข้อมูลโฆษณา'`
- `currentStatus: 'สถานะปัจจุบัน'`
- `currentShippingInfo: 'ข้อมูลการจัดส่งปัจจุบัน'`
- `currentRefusalReason: 'เหตุผลการปฏิเสธปัจจุบัน'`
- `approvalStatus: 'สถานะการอนุมัติ'`

## 影响范围
- `/samples/public` 页面的样品详情弹层
- 多语言切换功能（中文、英文、泰文）

## 同步状态
- [ ] 已同步到服务器
- [ ] 已验证功能正常

## 备注
修复了主人提出的两个问题：
1. 弹层没有多语种国际化 - 已修复
2. 默认展示"寄样状态"页签 - 已修复，默认显示寄样状态页签
# 待同步修改记录

**日期**: 2026-04-10
**状态**: 待同步

---

## 修改文件

1. `frontend/src/views/influencer-managements/Index.vue`
2. `frontend/src/views/mobile/Influencers.vue`
3. `frontend/src/views/mobile/Samples.vue`

---

## 修改内容

### 粉丝数统一改为K为单位展示

**规则**：
- 数据库存储保持不变（原始数字）
- 展示时除1000，加K后缀
- 输入时以K为单位，提交时乘1000存数据库

### 涉及位置

| 文件 | 位置 | 说明 |
|------|------|------|
| influencer-managements/Index.vue | 列表展示 | `latestFollowers` → `formatFollowers()` |
| influencer-managements/Index.vue | 维护记录表格 | `followers` → `formatFollowers()` |
| influencer-managements/Index.vue | 详情展示 | `latestFollowers` → `formatFollowers()` |
| influencer-managements/Index.vue | 筛选输入 | K为单位，提交时转换 |
| influencer-managements/Index.vue | 添加维护表单 | displayFollowers变量，输入K转存原始值 |
| Influencers.vue | 列表展示 | `latestFollowers` → `formatFollowers()` |
| Influencers.vue | 详情展示 | `latestFollowers` → `formatFollowers()` |
| Influencers.vue | 维护记录 | `followers` → `formatFollowers()` |
| Influencers.vue | 添加维护表单 | displayFollowers变量，输入K转存原始值 |
| Samples.vue | 详情展示 | `followerCount` → `formatFollowers()` |
| Samples.vue | 申请表单 | displayFollowerCount变量，输入K转存原始值 |

### 新增函数

```javascript
// 粉丝数格式化：展示时除1000，加K后缀
const formatFollowers = (value) => {
  if (!value && value !== 0) return '-'
  const k = value / 1000
  if (k >= 1) {
    return k % 1 === 0 ? `${k}K` : `${k.toFixed(1)}K`
  }
  return value.toString()
}

// 粉丝数反向转换：输入值乘1000存入数据库
const parseFollowersToDb = (value) => {
  if (!value && value !== 0) return 0
  return Math.round(value * 1000)
}
```

---

## 同步检查清单

- [ ] influencer-managements列表展示正常
- [ ] 维护记录表格显示正确（xx K格式）
- [ ] 详情页展示正确
- [ ] 筛选功能正常（输入K，查询原始值）
- [ ] 添加维护表单输入正常
- [ ] 移动端Influencers展示正常
- [ ] 移动端Samples申请和详情正常
- [ ] 数据库存储值验证（10K输入存10000）

# 样品提交记录显示BD信息

## 修改时间
2026-04-13 21:50

## 修改内容
在样品管理的"提交记录"中为每个记录添加了BD信息显示。

### 修改的文件
1. `frontend/src/views/samples/Management.vue`
   - 在第170行后添加了BD字段显示：
   ```html
   <div class="submission-cell">
     <span class="cell-label">BD：</span>
     <span class="cell-value">{{ sub.salesman || '-' }}</span>
   </div>
   ```

2. `frontend/src/views/samples/ManagementBDSelf.vue`
   - 在第126行后添加了BD字段显示：
   ```html
   <div class="submission-cell">
     <span class="cell-label">BD：</span>
     <span class="cell-value">{{ sub.salesman || '-' }}</span>
   </div>
   ```

3. `frontend/src/views/samples/PublicCollection.vue`
   - 修复了缩进问题（第284行）
   - 在第297行后添加了BD字段显示：
   ```html
   <div class="submission-cell">
     <span class="cell-label">BD：</span>
     <span class="cell-value">{{ sub.salesman || '-' }}</span>
   </div>
   ```

## 功能说明
- 在样品管理、BD自管理和公开样品收集页面中，为每个提交记录显示对应的BD信息
- 帮助用户快速识别每个提交记录对应的负责BD

## 影响范围
- 前端显示修改
- 不影响后端逻辑

## 同步状态
- [ ] 未同步
- [ ] 已同步

## 部署操作
需要：
1. 同步前端dist目录
2. Docker build前端容器

因为只修改了前端文件，不需要重建后端容器。
# 同步记录

## 修改时间
2026-04-15 09:44

## 修改文件
`server/migrate_sample_productId.js`

## 修改内容
修复SampleManagement现有数据时，同时修复shopId字段：
- 根据productId(ObjectId)查找Product表
- 获取Product的`tiktokProductId`和`shopId`
- 同时更新samplemanagements的`productId`和`shopId`字段

## 修改详情
```javascript
// 映射结构从
productIdMap[objectId] = p.tiktokProductId
// 改为
productIdMap[objectId] = {
  tiktokProductId: p.tiktokProductId,
  shopId: p.shopId
}

// 更新时同时修复两个字段
const updateFields = { productId: productInfo.tiktokProductId };
if (productInfo.shopId) {
  updateFields.shopId = productInfo.shopId;
}
```

## 同步状态
未同步（待主人授权后同步到线上）

## 运行命令
```bash
cd server && node migrate_sample_productId.js
```

# 阶段三数据库字段收敛完成 — 2026-07-14

## 概述
阶段三（数据库字段收敛）本地开发、测试验证、线上部署全部完成。

## 改动清单

### 模型层
| 文件 | 变更 |
|------|------|
| `server/models/ReportOrder.js` | 扩展 totalAmount/commissionRate/status（合并自 Order） |
| `server/models/Activity.js` | sampleMethod 枚举统一为 online/offline |
| `server/models/Commission.js` | orderId ref 改为 ReportOrder |
| `server/models/SampleManagement.js` | 删除 isSampleSent 字段及索引 |
| `server/models/Shop.js` | 删除无消费方 products 字段 |
| `server/models/index.js` | 移除 Order 导出 |
| `server/models/Order.js` | **删除**（已合并至 ReportOrder） |
| `server/models/Bill.js` | 移除 billNo 字段定义中 redundant unique:true |

### 路由层
| 文件 | 变更 |
|------|------|
| `server/routes/orders.js` | CRUD 改为 ReportOrder |
| `server/routes/commissions.js` | 佣金计算改为 ReportOrder |
| `server/routes/performance.js` | 报表生成改为 ReportOrder |
| `server/routes/samples.js` | isSampleSent → sampleStatus |
| `server/routes/initialization.js` | 导入适配 sampleStatus |
| `server/routes/public-samples.js` | 派生 isSampleSent 兼容前端 |

### 前端
| 文件 | 变更 |
|------|------|
| `frontend/src/views/activities/Index.vue` | sampleMethod el-option 改为 online/offline |
| `frontend/src/views/products/ProductTab.vue` | sampleMethod el-option 改为 online/offline |

### 迁移脚本（新增 4 个）
| 文件 | 用途 |
|------|------|
| `server/migrate_activity_samplemethod.js` | 中文枚举→online/offline |
| `server/migrate_sample_compat.js` | 兼容字段回填 influencerId |
| `server/migrate_order_to_reportorder.js` | Order→ReportOrder 数据合并 |
| `server/migrate_clean_fields.js` | 清理无消费方冗余字段 |

### 基础设施
| 文件 | 变更 |
|------|------|
| `Dockerfile` | 修复 npm ci → npm install --omit=dev（因 lock 陈旧） |
| `server/package-lock.json` | 重新生成干净 lock 文件 |

### 测试
| 文件 | 说明 |
|------|------|
| `server/smoke-test.cjs` | 新增冒烟测试（含订单合并闭环验证） |

## 线上执行记录

- **备份**: `/home/ubuntu/backups/tapdb_backup_20260714_193516.archive`（1.9M，全量）
- **迁移结果**:
  - activities 11/11 → online/offline，中文残留=0
  - products 351/351 → online/offline，中文残留=0
  - samplemanagements influencerId 回填 301 条（354条保留兼容字段）
  - order→reportorder 空操作（线上 orders=0）
  - clean_fields 空操作（无线冗余字段）
- **部署**: `docker compose build --no-cache` 重建前后端
- **验证**: 网站 HTTP 200，后端无报错

## 同步状态
- [ ] 待同步到其他环境
- [x] 生产环境已部署
- [x] Git 已提交 (c2dbc41)

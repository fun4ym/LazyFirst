# 样品申请页面添加店铺搜索条件

## 修改文件
1. `server/routes/samples.js` - 添加 shopId 筛选参数支持
2. `frontend/src/views/samples/Management.vue` - 添加店铺下拉搜索框
3. `frontend/src/i18n/zh.js` - 添加 selectShop 翻译
4. `frontend/src/i18n/en.js` - 添加 selectShop 翻译

## 修改内容

### 后端 (server/routes/samples.js)
- 在查询参数中添加 `shopId`
- 添加按店铺筛选的逻辑：通过 Product 表关联查询该店铺下的所有商品

### 前端 (frontend/src/views/samples/Management.vue)
- 添加 `shopList` 状态变量
- 添加 `loadShopList()` 函数加载店铺列表
- 在搜索表单中添加店铺下拉选择器（支持筛选）
- 在 `searchForm` 中添加 `shopId` 字段
- 在 `resetSearch` 中清空 `shopId`
- 在 `onMounted` 中调用 `loadShopList()`

## 同步状态
- [x] 已同步到服务器
- [x] 已验证网站正常访问 (HTTP 200)

## 部署时间
2026-04-15 16:30

## 部署方式
1. ✅ 备份线上数据库
2. ✅ 本地构建前端：`npm run build`
3. ✅ 同步代码（包含dist）
4. ✅ 重建并重启：`docker compose build --no-cache && docker compose up -d`
5. ✅ 验证网站访问：HTTP 200
